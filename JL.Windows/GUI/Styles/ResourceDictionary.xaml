<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:JL.Windows.GUI.CustomControls">

    <Style TargetType="ComboBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="{x:Type local:EffectWrapper}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:EffectWrapper}">
                    <Grid x:Name="DIRECTION_8">
                        <Grid x:Name="DIRECTION_7">
                            <Grid x:Name="DIRECTION_6">
                                <Grid x:Name="DIRECTION_5">
                                    <Grid x:Name="DIRECTION_4">
                                        <Grid x:Name="DIRECTION_3">
                                            <Grid x:Name="DIRECTION_2">
                                                <Grid x:Name="DIRECTION_1">
                                                    <ContentPresenter />
                                                </Grid>
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
