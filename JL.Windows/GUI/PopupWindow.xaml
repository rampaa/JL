<Window x:Class="JL.Windows.GUI.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Title="PopupWindow"
        Width="Auto" Height="Auto" SizeToContent="Height" MaxWidth="700" MaxHeight="520"
        Topmost="True" WindowStyle="None" AllowsTransparency="true" ShowInTaskbar="False"
        KeyDown="Window_KeyDown" Closing="Window_Closing" MouseEnter="OnMouseEnter" MouseLeave="OnMouseLeave"
        x:ClassModifier="internal">

    <ScrollViewer x:Name="PopUpScrollViewer" VerticalScrollBarVisibility="Auto"
                  VerticalContentAlignment="Top"
                  CanContentScroll="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Name="TextBlockMiningModeReminder" FontSize="13" Padding="5,1,0,1"
                       Visibility="Collapsed" TextWrapping="Wrap"
                       Background="{Binding Path=Background,
                  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                       Foreground="{Binding Path=Foreground,
                  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}">
            </TextBlock>

            <ItemsControl Grid.Row="1" Name="ItemsControlButtons" Visibility="Collapsed"
                          ItemsSource="{Binding Path=DictTypeButtons}"
                          Background="{Binding Path=Background,
                  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <ListBox Grid.Row="2" VirtualizingStackPanel.VirtualizationMode="Recycling" Focusable="False"
                     BorderThickness="0"
                     Background="{Binding Path=Background,
                  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                     PreviewMouseWheel="PopupListBox_PreviewMouseWheel"
                     x:Name="PopupListBox" ItemsSource="{Binding Path=ResultStackPanels}" Opacity="70">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Focusable" Value="False" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

        </Grid>
    </ScrollViewer>
    <Window.ContextMenu>
        <ContextMenu Name="PopupContextMenu">
            <MenuItem
                Name="AddNameButton"
                Header="Add name"
                Click="AddName" />
            <MenuItem
                Name="AddWordButton"
                Header="Add word"
                Click="AddWord" />
            <MenuItem
                Header="Copy"
                Command="Copy" />
            <MenuItem
                Name="SearchButton"
                Header="Search"
                Click="SearchWithBrowser" />
            <MenuItem
                Name="StatsButton"
                Header="Stats"
                Click="ShowStats" />
        </ContextMenu>
    </Window.ContextMenu>
</Window>
