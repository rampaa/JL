<Window x:Class="JL.Windows.GUI.Notification.CustomNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Focusable="False"
        ShowInTaskbar="False"
        ShowActivated="False"
        Title="CustomNotificationWindow" Width="500" Topmost="True" Background="{DynamicResource RegionBrush}"
        x:ClassModifier="internal">
    <Grid>
        <Border Name="CustomNotificationWindowBorder" BorderThickness="2">
            <TextBlock Name="CustomNotificationWindowTextBlock"
                       Focusable="False"
                       FontSize="25"
                       Opacity="0.8"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Left"
                       Style="{StaticResource TextBlockDefault}"
                       />
        </Border>
    </Grid>
</Window>
