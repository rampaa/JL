<Window x:Class="JL.Windows.GUI.Info.InfoDataGridWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d" Topmost="True"
        Title="" MaxHeight="600" MaxWidth="800" ResizeMode="NoResize" ShowInTaskbar="False" SizeToContent="WidthAndHeight" Background="{DynamicResource RegionBrush}"
        x:ClassModifier="internal" Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <hc:TextBox x:Name="InfoDataGridSearchTextBox" Grid.Row="0" HorizontalAlignment="Center" Width="300" Height="35" TextWrapping="Wrap"
                    hc:InfoElement.Placeholder="Search" Margin="5" TextChanged="InfoDataGridSearchTextBox_TextChanged"/>

        <DataGrid Name="InfoDataGrid" Grid.Row="1" IsReadOnly="True" Width="Auto" Height="Auto" AutoGenerateColumns="False"
                  CanUserSortColumns="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Term" Binding="{Binding Key}" Width="*" MaxWidth="400">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Count" Binding="{Binding Value}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="TextAlignment" Value="Left"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
