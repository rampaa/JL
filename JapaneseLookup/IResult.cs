namespace JapaneseLookup
{
    // should be a class but I made it an interface because we have enough "result"s already
    // because of the naming convention (I prefix), we can immediately tell what it is when we see it used in code
    public interface IResult
    {
    }
}